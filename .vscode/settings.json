// 将设置放入此文件中以覆盖默认值和用户设置。
{
    // 控制字体大小。
    "editor.fontSize": 16,
    // 配置 glob 模式以排除文件和文件夹。
    "files.exclude": {
        "**/.git": true,
        "**/.svn": true,
        "**/.hg": true,
        "**/.DS_Store": true,
        "**/node_modules": true
    },
    // 是否启用了 GIT
    "git.enabled": false,
    // File extensions that can be beautified as CSS.
    "beautify.CSSfiles": [
        "css",
        "less",
        "scss"
    ]
}